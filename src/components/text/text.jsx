export default function CustomText(input) {
    if(input.length <= 0) {
        console.log("Input's length <= 0");
        return;
    }
    let ouptut = new Array();
    for(let i = 0; i < input.length; i++) {
        ouptut.push(<p>e[0]</p>);
    }
    
    return ouptut;
}